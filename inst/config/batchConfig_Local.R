## For configuration instructions and examples, see the batchtools R package reference on CRAN. 

cluster.functions = makeClusterFunctionsInteractive(external = FALSE, write.logs = TRUE,
  fs.latency = 65)